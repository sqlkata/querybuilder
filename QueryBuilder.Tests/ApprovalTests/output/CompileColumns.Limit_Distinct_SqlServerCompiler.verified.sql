-------- ORIGINAL -----------

SELECT DISTINCT * 
FROM [X] 
ORDER BY (
SELECT 0) OFFSET 0 
ROWS FETCH NEXT 3 
ROWS ONLY

----------- RAW -------------

SELECT DISTINCT * 
FROM [X] 
ORDER BY (
SELECT 0) OFFSET ? 
ROWS FETCH NEXT ? 
ROWS ONLY

--------PARAMETRIZED --------

SELECT DISTINCT * 
FROM [X] 
ORDER BY (
SELECT 0) OFFSET @p0 
ROWS FETCH NEXT @p1 
ROWS ONLY