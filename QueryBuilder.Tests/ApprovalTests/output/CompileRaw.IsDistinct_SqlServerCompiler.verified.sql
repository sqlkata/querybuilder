-------- ORIGINAL -----------

SELECT MAX(*) AS [max] 
FROM (
SELECT DISTINCT [a]) AS [maxQuery]

----------- RAW -------------

SELECT MAX(*) AS [max] 
FROM (
SELECT DISTINCT [a]) AS [maxQuery]

--------PARAMETRIZED --------

SELECT MAX(*) AS [max] 
FROM (
SELECT DISTINCT [a]) AS [maxQuery]