-------- ORIGINAL -----------
WITH "Q" AS (
SELECT "A")

SELECT * 
FROM "X"

----------- RAW -------------
WITH "Q" AS (
SELECT "A")

SELECT * 
FROM "X"

--------PARAMETRIZED --------
WITH "Q" AS (
SELECT "A")

SELECT * 
FROM "X"