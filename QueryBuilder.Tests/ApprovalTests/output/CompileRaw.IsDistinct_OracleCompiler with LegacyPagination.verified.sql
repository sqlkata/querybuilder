-------- ORIGINAL -----------

SELECT MAX(*) "max" 
FROM (
SELECT DISTINCT "a") "maxQuery"

----------- RAW -------------

SELECT MAX(*) "max" 
FROM (
SELECT DISTINCT "a") "maxQuery"

--------PARAMETRIZED --------

SELECT MAX(*) "max" 
FROM (
SELECT DISTINCT "a") "maxQuery"